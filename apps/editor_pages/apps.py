from django.apps import AppConfig


class EditorPagesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.editor_pages'

    verbose_name = 'Страницы с редактором'
